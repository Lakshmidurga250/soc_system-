[project]
name = "sentinelai"
version = "0.1.0"
description = "Local AI-assisted security operations platform"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["backend"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
